<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thesis">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:background="@color/white"
        android:icon="@mipmap/ic_launcher"
        android:label="EESTEC AGH Kraków"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.Teams"></activity>
        <activity android:name=".view.OrganizationDetails" />
        <activity
            android:name=".view.DetailNews"
            android:label="Details"
            android:parentActivityName=".view.News" />
        <activity android:name=".view.Calendar" />
        <activity android:name=".view.ImportantLinks" />
        <activity android:name=".view.Projects" />
        <activity android:name=".view.Contacts" />
        <activity android:name=".view.ActivitySheet" />
        <activity android:name=".view.News" />
        <activity android:name=".view.Registration" />
        <activity android:name=".view.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>